# 颖图半自动标注软件使用说明书

## 1. 软件概述

颖图半自动标注软件用于图像目标标注工作，适合对一批图片进行矩形框标注、标签管理、自动辅助标注和人工补充修正。

软件支持四种标注模式：

- 自动：通过点击或拖拽矩形框，让软件自动生成目标标注。
- 手动：直接拖拽矩形框保存人工标注。
- 小目标：针对较小目标，在目标附近拖拽矩形框后进行局部辅助标注。
- 多目标：连续绘制多个提示框后，一次性执行多框推理，适合一张图中有多个同类目标的场景。

标注结果会自动保存到图片所在文件夹中。正常使用时，不需要手动编辑标注文件。

## 2. 使用前准备

### 2.1 图片文件夹

请将需要标注的图片放在同一个文件夹中。软件会读取常见图片格式，例如 JPG、PNG、BMP 等。

建议每个标注任务单独使用一个文件夹，便于管理图片和对应标注文件。

### 2.2 标签准备

软件右侧提供“标签列表”。开始标注前，请先确认标签列表中已有需要使用的类别。

如果没有合适标签，可以通过“添加标签”创建新标签。

### 2.3 模型初始化

软件启动后会尝试自动初始化模型。初始化完成后，状态栏会显示模型已就绪。

如果自动初始化失败，软件仍可正常打开图片并进行手动标注。需要使用自动、小目标或多目标模式时，可点击“初始化模型”再次尝试初始化。

## 3. 界面说明

### 3.1 顶部工具栏

顶部工具栏包含常用操作：

- 打开文件夹：选择需要标注的图片目录。
- 初始化模型：手动初始化辅助标注模型。
- 打开目录：在文件管理器中打开当前图片所在目录。
- 标注模式：选择自动、手动、小目标或多目标。
- 当前标签：选择新标注要使用的标签。
- 显示标签：控制画布中是否显示标注框的标签文字。

### 3.2 左侧图片列表

左侧显示当前文件夹中的图片。点击列表中的图片即可切换到对应图片。

### 3.3 中间标注画布

中间区域用于显示图片和标注框。用户可以在这里点击、拖拽、缩放和选择标注。

### 3.4 右侧标签与标注面板

右侧上方是标签列表，用于管理可用标签。

右侧下方是标注对象列表，用于查看当前图片已有标注，并可删除或修改标注标签。

### 3.5 底部导航栏

底部按钮用于图片切换和多目标操作：

- 第一张：跳转到第一张图片。
- 上一张：切换到上一张图片。
- 执行多框推理：在多目标模式下，对已绘制的多个提示框执行辅助标注。
- 清空多框：清空当前图片中尚未执行推理的多目标提示框。
- 删除图片：删除当前图片及对应标注文件。
- 下一张：切换到下一张图片。
- 最后一张：跳转到最后一张图片。

### 3.6 状态栏

窗口底部状态栏显示模型状态、图片信息、当前目录、当前图片和操作结果。

如果操作失败，请先查看状态栏提示。

## 4. 推荐标注流程

建议按照以下顺序完成一批图片的标注：

1. 点击“打开文件夹”，选择图片目录。
2. 等待模型初始化完成；如果失败，可点击“初始化模型”重试。
3. 在“当前标签”中选择要标注的类别。
4. 根据图片情况选择标注模式。
5. 在画布中完成标注。
6. 检查右侧标注对象列表和画布显示。
7. 需要时删除、修改或补充标注。
8. 点击“下一张”继续处理下一张图片。

标注会在操作过程中自动保存，不需要额外点击保存按钮。

## 5. 基础操作

### 5.1 打开图片文件夹

点击“打开文件夹”，在弹出的窗口中选择图片所在文件夹。

打开成功后，左侧图片列表会显示该文件夹中的图片，软件会自动加载第一张图片。

### 5.2 切换图片

可以通过以下方式切换图片：

- 点击左侧图片列表中的图片。
- 使用底部“第一张”“上一张”“下一张”“最后一张”按钮。
- 使用快捷键 A 切换上一张，D 切换下一张。

切换图片后，画布、标注对象列表和状态栏会同步刷新。

### 5.3 打开当前目录

点击“打开目录”，可以在文件管理器中打开当前图片所在文件夹。

### 5.4 显示或隐藏标签名

勾选“显示标签”时，画布中的标注框会显示标签名。

取消勾选后，画布只显示标注框，不显示标签文字。该操作只影响显示效果，不影响已保存的标注内容。

### 5.5 删除图片

点击“删除图片”会删除当前图片及对应标注文件。删除前软件会弹出确认提示。

请谨慎使用该功能，确认不再需要该图片后再删除。

## 6. 标签管理

### 6.1 添加标签

点击右侧“添加标签”，输入标签名称并选择颜色。添加成功后，新标签会出现在标签列表和“当前标签”下拉框中。

### 6.2 删除标签

在右侧标签列表中选中标签，点击“删除标签”即可删除。

删除标签不会自动删除已经保存的历史标注，但后续不建议继续使用已删除的标签名称。

### 6.3 选择当前标签

“当前标签”决定新建标注使用的类别。开始标注前，请确认当前标签选择正确。

### 6.4 修改已有标注标签

在右侧标注对象列表中选中一个标注，点击“修改标签”，然后选择新的标签。

修改完成后，画布和标注文件会同步更新。

## 7. 标注模式说明

### 7.1 自动模式

自动模式适合目标较清晰、希望借助软件快速生成标注的场景。

操作方式：

1. 在“标注模式”中选择“自动”。
2. 在“当前标签”中选择目标类别。
3. 在画布中点击目标附近，或拖拽一个矩形框圈出目标区域。
4. 等待软件生成标注结果。
5. 检查结果是否准确，必要时删除或手动补充。

如果模型未初始化，自动模式会提示无法推理。此时可点击“初始化模型”后重试，或切换到手动模式继续标注。

### 7.2 手动模式

手动模式适合需要人工精确控制标注框的位置和大小的场景。

操作方式：

1. 在“标注模式”中选择“手动”。
2. 在“当前标签”中选择目标类别。
3. 在画布中按住鼠标左键拖拽，框住目标。
4. 松开鼠标后，标注会自动保存。

手动模式不依赖模型初始化，即使模型未就绪也可以使用。

### 7.3 小目标模式

小目标模式适合图片中目标较小、直接自动标注效果不稳定的场景。

操作方式：

1. 在“标注模式”中选择“小目标”。
2. 在“当前标签”中选择目标类别。
3. 在小目标附近拖拽一个矩形框，尽量覆盖目标所在区域。
4. 等待软件完成局部辅助标注。
5. 检查结果，必要时重复拖框补充其他小目标。

小目标模式中，鼠标点击不会触发标注，请使用拖拽矩形框的方式操作。

### 7.4 多目标模式

多目标模式适合一张图中有多个目标，需要一次性处理多个提示区域的场景。

操作方式：

1. 在“标注模式”中选择“多目标”。
2. 在“当前标签”中选择目标类别。
3. 在画布中连续拖拽多个矩形框，分别覆盖需要处理的目标区域。
4. 确认提示框数量和位置无误后，点击“执行多框推理”。
5. 等待软件生成多个标注结果。
6. 如果还未执行推理，想重新选择提示框，可点击“清空多框”后重新绘制。

注意：

- 多目标模式下，拖拽出的框是待推理提示框，不会立刻保存为最终标注。
- 点击“执行多框推理”后，软件会根据提示框生成标注结果。
- 切换图片或切换模式后，未执行的多目标提示框会被清空。

## 8. 标注对象管理

### 8.1 查看标注

当前图片的所有标注会显示在右侧“标注对象”列表中。画布中也会显示对应的矩形框。

点击列表中的标注对象，画布中对应标注会高亮显示。

### 8.2 删除单个标注

在标注对象列表中选中一个标注，点击“删除标注”即可删除。

删除后，画布、列表和标注文件会同步更新。

### 8.3 修改标注标签

在标注对象列表中选中一个标注，点击“修改标签”，选择新的标签后确认。

### 8.4 清空当前图片标注

点击“清空当前图片标注”会删除当前图片的全部标注。

该操作只影响当前图片，不会影响其他图片。

## 9. 画布操作

### 9.1 缩放图片

在画布中滚动鼠标滚轮，可以放大或缩小图片。

缩放只影响查看效果，不影响标注坐标和保存结果。

### 9.2 重置视图

在画布中点击鼠标右键，可以重置图片视图。

### 9.3 拖拽标注框

在手动、自动、小目标、多目标模式中，拖拽矩形框的含义不同：

- 手动模式：拖拽框会直接保存为标注。
- 自动模式：拖拽框作为辅助提示，用于生成标注结果。
- 小目标模式：拖拽框作为局部标注提示。
- 多目标模式：拖拽框加入待推理框列表，点击“执行多框推理”后统一处理。

### 9.4 选择已有标注

可以在画布中点击已有标注框，也可以在右侧标注对象列表中选择标注。选中的标注会高亮显示。

## 10. 数据保存说明

软件会自动保存标注结果。通常情况下，用户不需要手动保存。

每张图片的标注会保存为同名标注文件，并放在图片所在文件夹中。

例如：

- 图片：`image001.jpg`
- 标注文件：`image001.json`

请注意：

- 不建议手动修改标注文件。
- 删除图片时，对应标注文件也会一起删除。
- 软件会自动处理重复标注框，减少同一目标被重复保存的情况。
- 如果标注数量与预期不同，可能是重复框被自动去重，属于正常情况。

## 11. 常见问题

### 11.1 打开软件后模型未初始化怎么办？

如果状态栏显示模型未初始化或初始化失败，可以点击“初始化模型”重试。

如果仍无法初始化，可以先使用手动模式完成标注。

### 11.2 为什么自动、小目标或多目标模式无法生成结果？

请检查以下事项：

- 模型是否已初始化完成。
- 当前是否已选择图片。
- 当前是否已选择标签。
- 目标是否过小、过模糊或框选区域不合适。

可以尝试重新框选目标区域，或切换到手动模式补充标注。

### 11.3 为什么多目标按钮不可用？

“执行多框推理”和“清空多框”只在多目标模式下使用。

如果按钮不可用，请确认：

- 当前模式是否为“多目标”。
- 是否已经在画布中绘制了至少一个待推理框。
- 软件当前是否正在执行推理。

### 11.4 为什么标签名不显示？

请检查顶部“显示标签”是否已勾选。

取消勾选后，画布只显示矩形框，不显示标签文字。

### 11.5 为什么标注数量会变化？

软件会自动处理重复标注框。如果多个框高度重叠，软件可能只保留其中一个，以减少重复标注。

这是正常现象，不代表标注丢失。

### 11.6 图片加载失败怎么办？

请确认图片文件没有损坏，并且文件格式是常见图片格式。

如果某张图片无法打开，可先跳过该图片，处理其他图片。

### 11.7 删除标注或图片后可以恢复吗？

软件内没有撤销恢复功能。删除标注、清空标注或删除图片前，请确认操作无误。

## 12. 操作建议

- 每次开始标注前，先确认当前标签是否正确。
- 自动生成的标注建议人工检查一遍。
- 小目标建议使用小目标模式逐个补充，避免一次框选过大区域。
- 多目标模式下，先确认所有提示框位置正确，再点击“执行多框推理”。
- 标注过程中留意状态栏提示，失败信息通常会显示在状态栏中。
- 不建议在软件运行时手动修改图片文件夹中的标注文件。

