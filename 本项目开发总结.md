# SemiAutoAnnoSoftware 项目开发总结

## 1. 项目定位

本项目是一个基于 Qt Widgets 的半自动图像标注软件，核心目标是把传统的图片标注工作流和 SAM3 自动推理结合起来，实现“打开图片目录、管理标签、手动框选、自动点/框提示推理、保存 LabelMe 风格 JSON 标注”的完整闭环。

当前工程保持 `MainWindow + ImageAnnotateWidget + SamInferenceBridge` 的主体架构：

- `MainWindow` 负责工作台布局、按钮/列表/状态栏、标签和标注对象管理。
- `ImageAnnotateWidget` 负责图像显示、缩放、坐标转换、框选、点击提示和标注绘制。
- `SamInferenceBridge` 负责对接 C++ SAM3/TensorRT 推理库。
- `SamInferenceWorker` 负责把模型初始化、设置当前图像、点/框推理放到后台线程执行。
- `AnnotationJsonIO` 和 `LabelConfigIO` 负责标注 JSON 与标签配置文件读写。

## 2. 使用的主要技术栈

### C++ 与构建

- C++17
- qmake `.pro` 工程
- MSVC 2019 64-bit 编译链
- Qt 5.15.2 Widgets

关键工程配置位于 `SemiAutoAnnoSoftware.pro`：

```pro
QT += core gui widgets svg
CONFIG += c++17
win32-msvc*: QMAKE_CXXFLAGS += /utf-8

SOURCES += \
    main.cpp \
    app/StartupOverlay.cpp \
    app/UiTheme.cpp \
    app/MainWindow.cpp \
    inference/SamInferenceWorker.cpp \
    widgets/ImageAnnotateWidget.cpp

LIBS += \
    -L$${SAM_LIB_DIR} \
    -lSamBaseLib \
    -lTrtSam3Lib

win32:LIBS += -ldwmapi -luser32
```

### Qt 库与模块

- `QtCore`：`QObject`、`QString`、`QDir`、`QFile`、`QTimer`、`QThread`、信号槽。
- `QtGui`：`QPainter`、`QPalette`、`QImage`、`QPixmap`、`QIcon`、`QKeySequence`。
- `QtWidgets`：`QMainWindow`、`QWidget`、`QSplitter`、`QListWidget`、`QStatusBar`、`QShortcut`、`QFileDialog`、`QMessageBox`。
- `QtSvg`：加载 SVG 图标和空状态插画资源。
- Windows DWM API：原生深色标题栏。
- C++ SAM3 推理库：应用层链接 `SamBaseLib`、`TrtSam3Lib`，通过 `TrtSam3` 完成 TensorRT 推理调用。

## 3. 主题渲染与商业化界面

### 3.1 QPalette + QSS 统一暗色主题

主题入口是 `UiTheme::apply(QApplication*)`，在 `main.cpp` 创建主窗口前执行。主题体系由三部分组成：

- `QPalette` 定义窗口、文本、按钮、高亮、tooltip 等基础颜色。
- `styles/dark.qss` 定义按钮、列表、状态栏、分割线、checkbox 等控件细节。
- `resources.qrc` 将 QSS、SVG 图标、空状态插画编译进 Qt 资源系统。

关键代码位于 `app/UiTheme.cpp`：

```cpp
QFont font = app->font();
font.setFamily(QStringLiteral("Microsoft YaHei UI"));
font.setPointSize(10);
app->setFont(font);

QPalette palette;
palette.setColor(QPalette::Window, QColor(20, 24, 31));
palette.setColor(QPalette::WindowText, QColor(229, 235, 245));
palette.setColor(QPalette::Base, QColor(15, 18, 24));
palette.setColor(QPalette::Button, QColor(37, 44, 56));
palette.setColor(QPalette::Highlight, QColor(51, 145, 255));
app->setPalette(palette);

QFile file(QStringLiteral(":/styles/dark.qss"));
app->setStyleSheet(QString::fromUtf8(file.readAll()));
```

资源入口位于 `resources.qrc`：

```xml
<file>styles/dark.qss</file>
<file>assets/images/empty-workspace.svg</file>
<file>assets/icons/folder-open.svg</file>
<file>assets/icons/cpu.svg</file>
<file>assets/icons/trash.svg</file>
```

### 3.2 Windows 原生深色标题栏

本项目没有做 frameless 自绘标题栏，而是保留系统边框、最小化、最大化、关闭按钮，并通过 Windows DWM 属性请求深色标题栏。

关键代码位于 `app/UiTheme.cpp`：

```cpp
const HWND hwnd = reinterpret_cast<HWND>(widget->winId());
const BOOL enabled = TRUE;
constexpr DWORD dwmwaUseImmersiveDarkMode = 20;
HRESULT hr = DwmSetWindowAttribute(hwnd, dwmwaUseImmersiveDarkMode, &enabled, sizeof(enabled));

if (FAILED(hr)) {
    constexpr DWORD dwmwaUseImmersiveDarkModeBefore20H1 = 19;
    DwmSetWindowAttribute(hwnd, dwmwaUseImmersiveDarkModeBefore20H1, &enabled, sizeof(enabled));
}

SetWindowPos(hwnd, nullptr, 0, 0, 0, 0,
             SWP_NOMOVE | SWP_NOSIZE | SWP_NOZORDER | SWP_NOOWNERZORDER | SWP_FRAMECHANGED);
RedrawWindow(hwnd, nullptr, nullptr, RDW_INVALIDATE | RDW_FRAME);
```

为了避免 Windows 首帧或标题更新后标题栏仍保持白色，`MainWindow` 在显示事件和标题更新后都会重新应用：

```cpp
void MainWindow::showEvent(QShowEvent* event) {
    QMainWindow::showEvent(event);
    applyNativeTitleBarTheme();
}

void MainWindow::applyNativeTitleBarTheme() {
    if (!isVisible()) {
        return;
    }

    UiTheme::applyDarkTitleBar(this);
    QTimer::singleShot(0, this, [this]() {
        UiTheme::applyDarkTitleBar(this);
    });
    QTimer::singleShot(120, this, [this]() {
        UiTheme::applyDarkTitleBar(this);
    });
}
```

## 4. 主界面工作台重排

主界面从传统表单式布局改造成三栏工作台：

- 左侧：图片列表。
- 中间：标注画布和底部图片导航栏。
- 右侧：标签列表、标注对象列表、标签/标注操作按钮。
- 顶部：紧凑 command bar，包含打开文件夹、初始化模型、打开目录、标注模式、当前标签、显示标签开关。
- 底部：状态栏显示模型状态、图像状态、目录和当前图片信息。

关键代码位于 `app/MainWindow.cpp`：

```cpp
auto* splitter = new QSplitter(Qt::Horizontal, ui->centralWidget);
splitter->setChildrenCollapsible(false);

auto* leftPanel = createPanel(splitter);
leftLayout->addWidget(ui->label_2);
leftLayout->addWidget(ui->imageList, 1);

auto* centerHost = new QWidget(splitter);
ui->imageWidget->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Expanding);
centerLayout->addWidget(ui->imageWidget, 1);

auto* rightPanel = createPanel(splitter);
```

这种方式的好处是 UI 更接近成熟商业软件的“工作台”形态，而且 `QSplitter` 允许用户按屏幕大小调整左右面板宽度。

## 5. 图标、按钮与资源体系

工具按钮使用 SVG 图标，按钮文本和 tooltip 统一在 `applyStaticTextAndIcons()` 中配置。图标通过 Qt Resource System 加载，不依赖运行目录里的散文件。

示例代码：

```cpp
configureActionButton(ui->openFolderButton,
                      QString::fromUtf8(u8"打开文件夹"),
                      QStringLiteral(":/assets/icons/folder-open.svg"),
                      QString::fromUtf8(u8"选择图片文件夹"));

configureActionButton(ui->initButton,
                      QString::fromUtf8(u8"初始化 模型"),
                      QStringLiteral(":/assets/icons/cpu.svg"),
                      QString::fromUtf8(u8"初始化 模型 推理模型"));
```

## 6. 标注画布显示与交互

### 6.1 自定义 QWidget + QPainter 绘制

`ImageAnnotateWidget` 是本项目的核心自定义显示控件。它没有使用 Qt 原生图片控件，而是重写 `paintEvent()`，使用 `QPainter` 绘制背景、图片、标注框、选中状态、临时推理结果、拖框提示和 HUD 信息。

关键代码位于 `widgets/ImageAnnotateWidget.cpp`：

```cpp
void ImageAnnotateWidget::paintEvent(QPaintEvent* event) {
    Q_UNUSED(event);

    QPainter painter(this);
    painter.setRenderHint(QPainter::Antialiasing, true);
    drawCanvasBackground(&painter);

    if (m_image.isNull()) {
        drawEmptyState(&painter);
        return;
    }

    const QRect display = imageDisplayRect();
    painter.drawImage(display, m_image);
}
```

### 6.2 深色网格背景、图片阴影与 HUD

画布背景使用深色渐变和网格线，让空白区域更有专业视觉层次：

```cpp
void ImageAnnotateWidget::drawCanvasBackground(QPainter* painter) {
    QLinearGradient bg(0, 0, 0, height());
    bg.setColorAt(0.0, QColor(13, 17, 24));
    bg.setColorAt(1.0, QColor(9, 12, 18));
    painter->fillRect(rect(), bg);

    painter->setPen(QPen(QColor(42, 51, 66, 85), 1.0));
    constexpr int grid = 32;
    for (int x = 0; x < width(); x += grid) {
        painter->drawLine(x, 0, x, height());
    }
}
```

### 6.3 标注框、选中控制柄、标签 badge

已有标注绘制为半透明填充 + 抗锯齿描边；选中对象使用高亮描边和四角控制柄：

```cpp
painter.setPen(QPen(drawColor, selected ? 3.0 : 2.0));
QColor fillColor = baseColor;
fillColor.setAlpha(selected ? 58 : 30);
painter.setBrush(fillColor);
painter.drawPolygon(widgetPoly);

if (selected) {
    painter.setPen(QPen(QColor(255, 214, 74, 210), 1.5));
    painter.setBrush(QColor(255, 214, 74));
    for (const QPointF& p : handles) {
        painter.drawRoundedRect(QRectF(p.x() - 4.0, p.y() - 4.0, 8.0, 8.0), 2, 2);
    }
}
```

### 6.4 标签显示开关

界面上的“显示标签”checkbox 只控制 label badge 是否显示，不影响矩形框绘制。实现方式是给 `ImageAnnotateWidget` 增加 `m_showAnnotationLabels` 状态。

```cpp
void ImageAnnotateWidget::setShowAnnotationLabels(bool show) {
    if (m_showAnnotationLabels == show) {
        return;
    }
    m_showAnnotationLabels = show;
    update();
}

if (m_showAnnotationLabels) {
    const QString tag = QStringLiteral("%1  %2").arg(i + 1).arg(ann.label);
    drawRoundedLabel(&painter, bgRect, drawColor, tag);
}
```

连接位于 `MainWindow::setupConnections()`：

```cpp
connect(ui->checkBox_HideLabel, &QCheckBox::toggled, ui->imageWidget,
        &ImageAnnotateWidget::setShowAnnotationLabels);
```

## 7. 快捷键与用户操作

图片切换支持按钮和快捷键两种方式。快捷键使用 `QShortcut + QKeySequence` 实现：

```cpp
QShortcut *lastImageShortcut = new QShortcut(QKeySequence(Qt::Key_A), this);
lastImageShortcut->setContext(Qt::WidgetWithChildrenShortcut);
connect(lastImageShortcut, &QShortcut::activated, this, &MainWindow::onPreviousImageClicked);

QShortcut *nextImageShortcut = new QShortcut(QKeySequence(Qt::Key_D), this);
nextImageShortcut->setContext(Qt::WidgetWithChildrenShortcut);
connect(nextImageShortcut, &QShortcut::activated, this, &MainWindow::onNextImageClicked);
```

其他常用操作包括：

- `QFileDialog::getExistingDirectory()` 打开图片文件夹。
- `QDesktopServices::openUrl()` 在系统资源管理器中打开当前目录。
- 鼠标滚轮缩放画布。
- 右键重置视图。
- 点击触发自动点提示。
- 拖拽创建矩形提示或手动矩形标注。

## 8. 启动动画与异步初始化

### 8.1 应用内 StartupOverlay

启动时主窗口立即显示，SAM3 初始化在后台进行；初始化期间使用 `StartupOverlay` 在主界面内部显示半透明遮罩和旋转进度环。

关键代码位于 `app/StartupOverlay.cpp`：

```cpp
StartupOverlay::StartupOverlay(QWidget* parent) : QWidget(parent), m_timer(new QTimer(this)) {
    setAttribute(Qt::WA_TransparentForMouseEvents, true);
    setAutoFillBackground(false);
    hide();

    connect(m_timer, &QTimer::timeout, this, &StartupOverlay::onAnimationTick);
}

void StartupOverlay::showMessage(const QString& title, const QString& detail) {
    m_title = title;
    m_detail = detail;
    raise();
    show();
    m_timer->start(24);
    update();
}
```

绘制层使用 `QPainter`：

```cpp
painter.fillRect(rect(), QColor(8, 11, 16, 168));
painter.drawArc(QRect(spinnerCenter.x() - radius, spinnerCenter.y() - radius,
                      radius * 2, radius * 2),
                m_angle * 16, 110 * 16);
```

### 8.2 启动后自动初始化

主窗口构造完成后，通过 `QTimer::singleShot(0, ...)` 把自动初始化推迟到事件循环开始后执行，避免构造阶段阻塞界面首帧显示：

```cpp
QTimer::singleShot(0, this, [this]() {
    startSamInitialization(true);
});
```

## 9. SAM3/TensorRT 推理接入

### 9.1 推理库接入方式

应用层通过 `SamInferenceBridge` 包装 `TrtSam3`。当前 Qt 工程不直接写 TensorRT engine 推理细节，而是链接外部 C++ 推理库：

- `SamBaseLib`
- `TrtSam3Lib`

桥接代码位于 `inference/SamInferenceBridge.cpp`：

```cpp
#include "trtsam3lib.h"

SamInferenceBridge::SamInferenceBridge() : m_sam3(new TrtSam3()) {}
```

模型目录默认读取程序目录下的 `sam3`：

```cpp
QString sam3ModelDir() {
    return QDir(QCoreApplication::applicationDirPath()).filePath(QStringLiteral("sam3"));
}
```

初始化前会检查必要 TensorRT engine 文件是否存在：

```cpp
const QList<QStringList> required = {
    {QStringLiteral("vision-encoder.fp16.trt"), QStringLiteral("vision-encoder.trt"), QStringLiteral("vision-encoder.engine")},
    {QStringLiteral("text-encoder.fp16.trt"), QStringLiteral("text-encoder.trt"), QStringLiteral("text-encoder.engine")},
    {QStringLiteral("geometry-encoder.fp16.trt"), QStringLiteral("geometry-encoder.trt"), QStringLiteral("geometry-encoder.engine")},
    {QStringLiteral("decoder.fp16.trt"), QStringLiteral("decoder.trt"), QStringLiteral("decoder.engine")}
};
```

### 9.2 点提示与框提示推理

点提示：

```cpp
SamInferResult SamInferenceBridge::inferByPoint(const QPointF& imagePoint, const QString& labelName) {
    SamInferResult result;
    if (!validateReady(&result)) {
        return result;
    }

    result = m_sam3->inferByPoint(imagePoint, labelName);
    return result;
}
```

框提示：

```cpp
SamInferResult SamInferenceBridge::inferByRect(const QRectF& imageRect, const QString& labelName) {
    SamInferResult result;
    if (!validateReady(&result)) {
        return result;
    }

    const QRectF normalized = imageRect.normalized();
    result = m_sam3->inferByRect(normalized, labelName);
    return result;
}
```

### 9.3 后台线程推理

为了避免模型初始化和推理卡住主界面，项目新增 `SamInferenceWorker`，并将其移动到独立 `QThread`。模型对象由 worker 独占，初始化、设置当前图片、点推理、框推理都通过 queued signal/invoke 进入同一线程。

关键代码位于 `app/MainWindow.cpp`：

```cpp
m_inferenceThread = new QThread(this);
m_inferenceWorker = new SamInferenceWorker();
m_inferenceWorker->moveToThread(m_inferenceThread);

connect(m_inferenceThread, &QThread::finished, m_inferenceWorker, &QObject::deleteLater);
connect(m_inferenceWorker, &SamInferenceWorker::initializeFinished,
        this, &MainWindow::onSamInitializeFinished);
connect(m_inferenceWorker, &SamInferenceWorker::pointInferenceFinished,
        this, &MainWindow::onSamPointInferenceFinished);

m_inferenceThread->start();
```

推理请求通过 `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 投递到 worker 线程：

```cpp
QMetaObject::invokeMethod(m_inferenceWorker, "inferByPoint", Qt::QueuedConnection,
                          Q_ARG(QPointF, imagePoint),
                          Q_ARG(QString, labelName),
                          Q_ARG(QString, m_currentImagePath));
```

Worker 内部负责捕获异常，并通过 signal 把结果返回主线程：

```cpp
void SamInferenceWorker::inferByPoint(const QPointF& imagePoint,
                                      const QString& labelName,
                                      const QString& imagePath) {
    SamInferResult result;
    try {
        result = m_bridge.inferByPoint(imagePoint, labelName);
    } catch (const std::exception& ex) {
        result.success = false;
        result.errorMessage = QStringLiteral("Infer by point exception: %1").arg(ex.what());
        qWarning().noquote() << "[InferByPoint]" << result.errorMessage;
    }
    emit pointInferenceFinished(result, labelName, imagePath);
}
```

这种设计保证：

- 主线程不被模型初始化阻塞。
- 推理期间 UI 仍可刷新状态栏、遮罩、列表和画布。
- 模型对象不跨线程反复使用，降低线程安全风险。
- 推理失败不会静默吞掉，会返回 `errorMessage` 并写日志。

## 10. 标注数据与标签配置

### 10.1 标注 JSON

标注文件采用图片同名 `.json` 文件保存，字段结构接近 LabelMe：

- `imagePath`
- `imageWidth`
- `imageHeight`
- `imageData`
- `shapes`
- 每个 shape 包含 `label`、`shape_type`、`points`

关键代码位于 `data/AnnotationJsonIO.cpp`：

```cpp
QJsonObject shapeObjectFromAnnotation(const AnnotationObject& annotation) {
    QJsonObject obj;
    obj.insert(QStringLiteral("label"), annotation.label);
    obj.insert(QStringLiteral("shape_type"), QStringLiteral("rectangle"));
    obj.insert(QStringLiteral("points"), pointsArrayFromRectPolygon(annotation.rectPolygonImage));
    return obj;
}

const QJsonDocument doc(root);
file.write(doc.toJson(QJsonDocument::Indented));
```

读写失败会通过 `errorMessage` 明确返回：

```cpp
if (!file.open(QIODevice::WriteOnly | QIODevice::Truncate)) {
    if (errorMessage) {
        *errorMessage = QStringLiteral("Failed to write json: %1").arg(jsonPath);
    }
    return false;
}
```

### 10.2 标签配置

标签配置由 `DefineLabel.json` 管理，使用 `LabelConfigIO` 读取/保存：

```cpp
config->infoSet = root.value(QStringLiteral("InfoSet")).toString();

for (const QJsonValue& v : root.value(QStringLiteral("NameList")).toArray()) {
    if (v.isString()) {
        config->nameList.push_back(v.toString());
    }
}

for (const QJsonValue& v : root.value(QStringLiteral("ClorDefine")).toArray()) {
    config->colorDefine.push_back(v.toInt(0x00FF00));
}
```

保存时继续保持原有字段名，避免破坏历史配置兼容性：

```cpp
root.insert(QStringLiteral("InfoSet"), normalized.infoSet);
root.insert(QStringLiteral("NameList"), nameList);
root.insert(QStringLiteral("ClorDefine"), colorList);
```

## 11. 错误处理与日志策略

项目遵循“不写假结果、不静默吞错”的原则。典型策略：

- 文件不存在、JSON 格式错误、模型目录缺失、engine 文件缺失、模型初始化异常、推理异常都会返回明确错误。
- UI 层通过 `statusBar()->showMessage(...)` 给用户反馈。
- 开发层通过 `appendLog(...)` 和 `qWarning()` 输出上下文信息。

示例：

```cpp
if (!validateSam3ModelDir(modelDir, &validationError)) {
    if (errorMessage) {
        *errorMessage = validationError;
    }
    qWarning().noquote() << "[SAM3 Init]" << validationError;
    return false;
}
```

推理前也会检查模型和当前图片状态：

```cpp
if (!m_initialized || !m_sam3 || !m_sam3->isInitialized()) {
    result->errorMessage = QStringLiteral("SAM3 is not initialized");
    qWarning().noquote() << "[SAM3 Infer]" << result->errorMessage;
    return false;
}
if (m_currentImage.isNull()) {
    result->errorMessage = QStringLiteral("Current image is not set");
    qWarning().noquote() << "[SAM3 Infer]" << result->errorMessage;
    return false;
}
```

## 12. 当前已实现的主要功能清单

- 深色商业化主题：`QPalette + QSS + SVG icons + qrc`。
- Windows 原生深色标题栏：`DwmSetWindowAttribute + SetWindowPos + RedrawWindow`。
- 三栏工作台布局：`QSplitter` 左图像列表、中画布、右标签/标注对象。
- 顶部 command bar：常用命令、模式选择、当前标签、显示标签开关。
- 底部图片导航：第一张、上一张、删除图片、下一张、最后一张。
- 状态栏：模型状态、图像状态、目录/图片摘要。
- 自定义标注画布：`QPainter` 绘制网格背景、图片阴影、空状态、HUD、标注框、选中控制柄。
- 标签显示开关：checkbox 控制 label badge 显示，不影响矩形框。
- 快捷键翻页：`QShortcut + QKeySequence`，A/D 切换上一张/下一张。
- 标签管理：添加标签、删除标签、保存颜色配置。
- 标注对象管理：删除、修改标签、清空当前图片标注。
- JSON 标注读写：图片同名 `.json`，保存 rectangle points。
- SAM3 自动初始化：启动后自动后台初始化，失败后主界面仍可用。
- SAM3 手动初始化：保留 initButton，失败可再次手动初始化。
- SAM3 点/框推理：自动模式下点击或拖框触发推理。
- 后台推理线程：`SamInferenceWorker + QThread`，避免 UI 卡死。
- 异常边界：模型缺失、文件失败、推理失败都有明确 `errorMessage` 和日志。

## 13. 后续可继续优化的方向

- 对顶部和底部工具按钮统一设置 `Qt::NoFocus`，隐藏键盘焦点虚线框，让工具栏视觉更干净。
- 为推理中的画布状态增加轻量 loading 标记，区分“模型初始化中”和“单次推理中”。
- 增加撤销/重做栈，提升手动标注效率。
- 增加多选标注、批量改标签、批量删除。
- 把常用快捷键集中到一个配置表，后续可做快捷键设置界面。
- 增加模型路径配置界面，避免强依赖程序目录下固定 `sam3` 文件夹。
- 增加自动保存状态提示，清晰区分“已保存/保存失败/未保存变更”。

